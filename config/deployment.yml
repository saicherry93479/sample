

apiVersion: apps/v1
kind: Deployment
metadata:
  name: flaskstaging
  labels:
    app: flaskstaging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flaskstaging
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: flaskstaging
    spec:
      containers:
      - name: flaskstaging
        image: <IMAGE>
        ports:
        - containerPort: 5000
        resources:
          requests:
            cpu: 100m
          limits:
            cpu: 100m
---
apiVersion: v1
kind: Service
metadata:
  name: flaskstagingservice
spec:
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000
  selector:
    app: flaskstaging
